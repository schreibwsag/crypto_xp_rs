
# CONTRIBUTING

- Use `rustfmt` and `clippy` before PRs.
- Add unit tests (A3) for all new code paths.
- Follow the security policy; never add logs that could expose secrets.
- Keep public API changes minimal and documented in CHANGELOG.
